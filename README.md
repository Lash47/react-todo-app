# React To-Do App

A to-do list built with React and Vite. Tasks are saved in localStorage.

## Run locally
npm install
npm run dev

## Features
- Add, complete, and delete tasks
- Filter by All, Active, or Done
- Clear completed tasks
- Tasks persist after refresh
